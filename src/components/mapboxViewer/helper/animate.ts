// import mapboxgl from 'react-';

// function Animate(map) {
    // we need map
    // need array of collection_items/features


    // algorithm
    // for each item in collection_items
        // have a fixed sized window of size k.
        // move the window from the initial till the last.
        // on each move, clean the previous one.

    // Note: we will have a callback that will be called for each tick
        // we will get the:
            // array of items.
            // index of the tick wrt item. can be datetime.
            // map object.

    // given the index and array of items, 
    // we need to display the item at that index
    // then load 3 more items to the right
// }


export {}